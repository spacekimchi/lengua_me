<div>
  <%= render partial: 'topics/partials/breadcrumbs' %>
  <h1>All Topics</h1>
  <% @topics.each do |topic| %>
    <%= link_to topic.name.titleize, topic_path(urlify(topic.name)) %>
  <% end %>
</div>
